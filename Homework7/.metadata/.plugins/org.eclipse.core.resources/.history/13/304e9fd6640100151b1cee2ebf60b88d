
public class Zadachka7 {

	public static void main(String[] args) {
		
		int factoriel = factoriel(5);
        System.out.printf("%d! = %d%n", 5, factoriel);

	}
	
	public static int factoriel(int number) {
	    if (number == 0) {
	    	return 1;
	    	
	    } else {
		      return number * factoriel(number - 1);
	    }
	}
}
